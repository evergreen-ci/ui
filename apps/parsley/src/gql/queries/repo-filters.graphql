query RepoFilters($repoRefId: String!) {
  repoSettings(repoId: $repoRefId) {
    projectRef {
      id
      parsleyFilters {
        caseSensitive
        exactMatch
        expression
      }
    }
  }
}
