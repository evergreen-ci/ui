fragment BaseTask on Task {
  displayName
  displayStatus
  execution
  id
  patchNumber
  versionMetadata {
    id
    isPatch
    message
    projectIdentifier
    projectMetadata {
      id
      parsleyFilters {
        caseSensitive
        exactMatch
        expression
      }
    }
    revision
  }
}
