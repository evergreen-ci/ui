fragment BaseTask on Task {
  displayName
  displayStatus
  execution
  id
  patchNumber
  versionMetadata {
    id
    isPatch
    message
    projectMetadata {
      id
      identifier
      repoRefId
    }
    revision
  }
}
