fragment BaseTask on Task {
  id
  displayName
  displayStatus
  execution
  patchNumber
  versionMetadata {
    id
    isPatch
    message
    projectIdentifier
    projectMetadata {
      id
    }
    revision
  }
}
