fragment UpstreamProject on Version {
  id
  upstreamProject {
    owner
    project
    repo
    revision
    task {
      execution
      id
    }
    triggerID
    triggerType
    version {
      id
    }
  }
}
