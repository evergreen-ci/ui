fragment UpstreamProject on Version {
  id
  upstreamProject {
    owner
    project
    repo
    revision
    task {
      id
      execution
    }
    triggerID
    triggerType
    version {
      id
    }
  }
}
