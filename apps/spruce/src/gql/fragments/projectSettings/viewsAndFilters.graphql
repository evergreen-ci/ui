fragment ProjectViewsAndFiltersSettings on Project {
  id
  parsleyFilters {
    caseSensitive
    exactMatch
    expression
  }
}

fragment RepoViewsAndFiltersSettings on RepoRef {
  id
  parsleyFilters {
    caseSensitive
    exactMatch
    expression
  }
}
