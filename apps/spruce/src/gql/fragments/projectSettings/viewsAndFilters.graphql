fragment ProjectViewsAndFiltersSettings on Project {
  id
  parsleyFilters {
    caseSensitive
    exactMatch
    expression
  }
  projectHealthView
}

fragment RepoViewsAndFiltersSettings on RepoRef {
  id
  parsleyFilters {
    caseSensitive
    exactMatch
    expression
  }
}
