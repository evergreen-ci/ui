fragment ProjectViewsAndFiltersSettings on Project {
  id
  parsleyFilters {
    caseSensitive
    description
    exactMatch
    expression
  }
}

fragment RepoViewsAndFiltersSettings on RepoRef {
  id
  parsleyFilters {
    caseSensitive
    description
    exactMatch
    expression
  }
}
