# eslint-disable @graphql-eslint/executable-definitions

# For defining local-only fields
extend type Task {
  reviewed: Boolean
}

type CostData {
  onDemandRate: Float
  savingsPlanRate: Float
}

extend type Distro {
  costData: CostData
}
