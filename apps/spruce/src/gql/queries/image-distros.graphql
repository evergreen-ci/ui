query ImageDistros($imageId: String!) {
  image(imageId: $imageId) {
    distros {
      hostAllocatorSettings {
        maximumHosts
      }
      name
      provider
      providerSettingsList
    }
    id
  }
}
