query TaskOwnerTeamsForTask($taskId: String!, $execution: Int) {
  task(taskId: $taskId, execution: $execution) {
    id
    execution
    taskOwnerTeam {
      assignmentType
      jiraProject
      messages
      teamName
    }
  }
}
