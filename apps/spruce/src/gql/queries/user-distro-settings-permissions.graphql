query UserDistroSettingsPermissions($distroId: String!) {
  user {
    permissions {
      canCreateDistro
      distroPermissions(options: { distroId: $distroId }) {
        id: distroId
        admin
        edit
      }
    }
    userId
  }
}
