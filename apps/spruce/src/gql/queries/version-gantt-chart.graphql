query VersionGanttChart(
  $versionId: String!
  $buildVariantsOptions: BuildVariantOptions!
) {
  version(versionId: $versionId) {
    id
    buildVariants(options: $buildVariantsOptions) {
      displayName
      tasks {
        id
        dependsOn {
          buildVariant
          name
        }
        displayName
        execution
        finishTime
        startTime
      }
      variant
    }
  }
}
