query TaskTestSample($tasks: [String!]!, $filters: [TestFilter!]!) {
  taskTestSample(taskIds: $tasks, filters: $filters) {
    execution
    matchingFailedTestNames
    taskId
    totalTestCount
  }
}
