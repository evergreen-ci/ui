query DistroTaskQueue($distroId: String!) {
  distroTaskQueue(distroId: $distroId) {
    id
    activatedBy
    buildVariant
    displayName
    expectedDuration
    priority
    project
    projectIdentifier
    requester
    version
  }
}
