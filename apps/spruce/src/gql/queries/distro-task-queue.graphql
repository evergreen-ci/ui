query DistroTaskQueue($distroId: String!) {
  distroTaskQueue(distroId: $distroId) {
    activatedBy
    buildVariant
    displayName
    expectedDuration
    id
    priority
    project
    projectIdentifier
    requester
    version
  }
}
