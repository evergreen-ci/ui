query BaseVersionAndTask($taskId: String!) {
  task(taskId: $taskId) {
    baseTask {
      execution
      id
      status: displayStatus
    }
    buildVariant
    displayName
    execution
    id
    projectIdentifier
    status: displayStatus
    versionMetadata {
      baseVersion {
        id
        order
      }
      id
      isPatch
    }
  }
}
