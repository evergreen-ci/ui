query DisplayTask($taskId: String!, $execution: Int) {
  task(taskId: $taskId, execution: $execution) {
    id
    displayName
    displayTask {
      id
      execution
    }
    execution
    executionTasks
  }
}
