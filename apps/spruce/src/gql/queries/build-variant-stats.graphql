query BuildVariantStats($id: String!) {
  version(versionId: $id) {
    buildVariantStats(options: {}) {
      displayName
      statusCounts {
        count
        status
      }
      variant
    }
    id
  }
}
