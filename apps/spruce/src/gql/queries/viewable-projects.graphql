query ViewableProjectRefs {
  viewableProjectRefs {
    groupDisplayName
    projects {
      id
      displayName
      enabled
      identifier
      isFavorite
      owner
      repo
    }
    repo {
      id
    }
  }
}
