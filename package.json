{
  "name": "evergreen-ui",
  "repository": "https://github.com/evergreen-ci/ui",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "prepare": "husky",
    "storybook": "concurrently \"pnpm --filter spruce run storybook --no-open\" \"pnpm --filter parsley run storybook --no-open\" \"pnpm --filter @evg-ui/lib run storybook --no-open\" \"pnpm --filter @evg-ui/fungi run storybook --no-open\" \"sleep 5 && storybook dev\"",
    "test": "vitest --typecheck=false --exclude=./**/snapshot.test.ts"
  },
  "devDependencies": {
    "@evg-ui/storybook-addon": "workspace:*",
    "@storybook/react-vite": "^9.0.18",
    "concurrently": "8.2.2",
    "husky": "9.1.7",
    "lint-staged": "16.1.5",
    "storybook": "^9.0.18",
    "vitest": "^3.2.4"
  }
}
