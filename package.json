{
  "name": "evergreen-ui",
  "repository": "https://github.com/evergreen-ci/ui",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "check-types": "pnpm -r run check-types",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "codegen": "pnpm -r codegen",
    "eslint:strict": "pnpm -r run eslint:strict",
    "eslint:fix": "pnpm -r run eslint:fix",
    "prepare": "husky",
    "snapshot": "pnpm -r run snapshot",
    "storybook": "pnpm --filter spruce --filter parsley --filter @evg-ui/* --parallel run storybook --no-open & sleep 5 && storybook dev",
    "test": "vitest --typecheck=false --exclude=./**/snapshot.test.ts"
  },
  "devDependencies": {
    "@evg-ui/storybook-addon": "workspace:*",
    "@storybook/react-vite": "^10.1.10",
    "husky": "9.1.7",
    "lint-staged": "16.1.5",
    "storybook": "^10.1.10",
    "vitest": "^3.2.4"
  }
}
